apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "open-catalog-api.fullname" . }}-test-connection"
  labels:
    {{- include "open-catalog-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "open-catalog-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
