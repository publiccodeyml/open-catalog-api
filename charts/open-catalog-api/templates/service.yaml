apiVersion: v1
kind: Service
metadata:
  name: {{ include "open-catalog-api.fullname" . }}
  labels:
    {{- include "open-catalog-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "open-catalog-api.selectorLabels" . | nindent 4 }}
